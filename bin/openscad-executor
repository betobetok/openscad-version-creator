#!/usr/bin/env php
<?php

require __DIR__ . '/../vendor/autoload.php';
define('ROOT_DIR', __DIR__ . '/..');

use VersionCreator\Executor;
use VersionCreator\Version;

//$executor = new Executor();

$file = '../dados/dice5-1.json';
$verJson = new Version($file);
echo json_encode($verJson);
